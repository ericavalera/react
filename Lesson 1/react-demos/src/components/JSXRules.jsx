import React from 'react'

const JSXRules = () => {
  return (
    <div>
      <h1>JSX Rules</h1>
      <p>
        <ul>
        <li>JSX must return a <b>single parent element.</b></li>
        <li>JSX elements must be <b>properly closed.</b></li>
        <li>JSX attributes are written using <b>camelCasel</b> (e.g., className instead of class).</li>
        </ul>
        </p>
    </div>
  );
};

export default JSXRules;
